# Implemented Features

**Single source of truth:** Implemented features are documented in **`docs/project_features.md`**, section **14. Implemented Features (from backlog)**.

That section lists what was moved from `improvements_backlog.md` after verification in the codebase (pretrained encoder, training infrastructure, evaluation/visualization, MLflow, representative-tile viz, literature search). For the full project inventory and purpose of each area, see `project_features.md`.
