# Daily Diary - Wednesday 25 February 2026

## Context

Current date: **2026-02-25**. Last diary: 2026-02-24 (production runs with slope-stripes, best c01b7cc9 val_loss 0.297; throttle changed to time-based; e2e minimal config fix).

We are on the **real production dataset** (512×512, slope-stripes channel, segmentation layer). Today we ran production training with **`--max-tiles 300`** (and later 600) to iterate faster; the run with 300 tiles used the **current** `training_config.yaml` (lr 3e-6, 150 epochs, subsample 0.6, early_stop 20).

---

## Model runs (since 2026-02-24)

Experiment `586083506121040615`. Runs from 24–25 Feb:

| Run ID (short) | best val_loss | best val_mae | best val_iou | lr     | epochs | loss | slope_stripes | train_subsample | early_stop | train_tiles | notes |
|----------------|---------------|--------------|--------------|--------|--------|------|---------------|----------------|------------|-------------|------|
| **`f8a087df`** | **0.289**      | 0.208        | 1.0          | 1e-5   | 35     | bce  | true          | 0.6             | 30         | 420         | Best so far (completed after 24 Feb diary) |
| `c01b7cc9`     | 0.297         | 0.214        | 1.0          | 5e-5   | 35     | bce  | true          | 0.3             | 30         | —           | Best in 24 Feb diary |
| `bac0c39d`     | 0.396         | 0.304        | 1.0          | 3e-6   | 150    | bce  | true          | 0.6             | 20         | 210         | **Today**: `--max-tiles 300`, current config; early stopped epoch 69 |

**Best completed run (all time in this window):** **f8a087df** — val_loss **0.289**, val_mae 0.208. lr 1e-5, 35 epochs, subsample 0.6, early_stop 30.

**Today’s run (bac0c39d):** Production with `--max-tiles 300` (210 train tiles after split/subsample). Config from `training_config.yaml`: lr 3e-6, 150 epochs, subsample 0.6, early_stop 20. Best val_loss **0.396** at an early epoch; then val_loss oscillated and early stopping fired at epoch 69. **Did not improve** on f8a087df or c01b7cc9.

### Parameter differences (what we tinkered with)

- **f8a087df vs c01b7cc9:** Lower lr (1e-5 vs 5e-5), higher subsample (0.6 vs 0.3); f8a087df reached 0.289.
- **bac0c39d vs best:** Much lower lr (3e-6), many more epochs (150), same subsample 0.6 as f8a087df, but **fewer tiles** (300 cap → 210 train). Result: worse val_loss (0.396). Suggests either (a) 3e-6 is too low for this setup, or (b) 300 tiles is too few for the current config, or both.

### Loss chart – today’s run (bac0c39d)

Val loss improved to ~0.396 then plateaued/oscillated; early stop at 69/150.

![Run bac0c39d — Loss](plots/2026-02-25_loss_bac0c39d.png)

---

## Summary vs 2026-02-24

- **Best run:** f8a087df (0.289) beats c01b7cc9 (0.297) and today’s bac0c39d (0.396).
- **Did we improve?** Yes relative to 24 Feb diary: the **best** production run we know of is now f8a087df at 0.289 (that run had completed after the 24 Feb diary was written). Today we did **not** improve: bac0c39d (current config + --max-tiles 300) is worse.
- **Problem solved?** No new bug fixed today. We **observed** that current `training_config.yaml` with `--max-tiles 300` does not match the best runs; better results so far with lr 1e-5 or 5e-5, 35 epochs, early_stop 30.
- **Next steps (for future days):** Consider re-running with lr 1e-5 or 5e-5 and early_stop 30 (e.g. from f8a087df/c01b7cc9), or try --max-tiles 600+ with current config to see if more data helps.

---

## README update

- **`--max-tiles`** for `train_model.py` was not documented. Added to the Training section under optional arguments: cap total tiles before train/val/test split for quick runs.

---

## E2E test fix

- **test_minimal_training_run_and_metrics** was failing with `assert 'best_val_loss' in {}`: it took the first run from `search_runs(max_results=1)`, which can be an in-progress or unrelated run (no metrics yet). Fixed by searching up to 20 runs ordered by `attributes.start_time DESC` and picking the first run that has `best_val_loss` in metrics.

---

## Endday

- Diary entry for **Wednesday 25 February 2026**. Date verified: **2026-02-25** (check-datetime skill).
- Graphics: loss plot for bac0c39d copied to `plots/2026-02-25_loss_bac0c39d.png` and embedded.
- E2E: minimal training test fixed (find run with best_val_loss); full e2e suite run.
- README: documented `--max-tiles` for `train_model.py`.
- Changes to be pushed.
